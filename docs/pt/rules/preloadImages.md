# preloadImages(doc)
>Analisa a página e retorna um array de URLs de imagens que precisam ser pré‑carregadas.

Exemplo:
--
```json
"preloadImages": "return ['1.jpg']"
```

*Garanta que a opção preload esteja habilitada*
