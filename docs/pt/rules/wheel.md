# wheel
>Defina como true para que a ação de próxima página só ocorra quando a roda do mouse for usada.

Exemplo:
--
```json
"wheel": true
```

*Às vezes a página rola automaticamente ao clicar em “carregar mais” e entra em loop; wheel=true evita isso.*
