# preloadImages(doc)
>Analysiert die Seite und gibt ein Array der Bild‑URLs zurück, die vorab geladen werden sollen.

Beispiel:
--
```json
"preloadImages": "return ['1.jpg']"
```

*Stelle sicher, dass die preload‑Option aktiviert ist*
