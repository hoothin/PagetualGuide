# lockScroll
>Establécelo en true si no quieres que la página haga scroll automático al navegar a la siguiente página.

Ejemplo:
--
```json
"lockScroll": true
```

*A veces la página hace scroll automático al pasar a la siguiente página; con true lo evitas.*
