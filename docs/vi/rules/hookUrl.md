# hookUrl
>Một số phần tử không phải link vẫn mở URL khi click và có thể mất chức năng sau khi chèn. Gắn selector để giữ chức năng click‑to‑open.

Ví dụ:
--
```json
"hookUrl": "#click-to-open"
```
>Giữ chức năng click cho phần tử `#click-to-open`.
