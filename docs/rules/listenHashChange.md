# listenHashChange
>Set to true so Pagetual restarts when the hash changes.

Example:
--
```json
"listenHashChange": true
```
>Restart when the URL changes from `http://abc.xxx/#123` to `http://abc.xxx/#456`.
