# pageNum
>Указывает номер страницы в текущем URL с помощью $p. В {} можно вычислять строку результата, например {$p*25+1}.

Пример:
--
```json
"pageNum": "&start={15*($p-1)}"
```
>Считает номер страницы как 2, когда URL — "&start=15".
