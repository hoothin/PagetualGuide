# manualMode
>Auf true setzen, um den manuellen Modus zu aktivieren. Paging stoppt, und die rechte Pfeiltaste (oder das Event `pagetual.next`) wird an den nextLink‑Klick gebunden.

Beispiel:
--
```json
"manualMode": true
```
