# preloadImages(doc)
>Анализирует страницу и возвращает массив URL изображений, которые нужно предзагрузить.

Пример:
--
```json
"preloadImages": "return ['1.jpg']"
```

*Убедитесь, что опция preload включена*
