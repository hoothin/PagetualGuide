# listenHashChange
>Establécelo en true para que Pagetual se reinicie cuando cambie el hash.

Ejemplo:
--
```json
"listenHashChange": true
```
>Reinicia Pagetual cuando la URL cambia de `http://abc.xxx/#123` a `http://abc.xxx/#456`.
