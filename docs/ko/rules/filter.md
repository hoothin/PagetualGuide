# filter
>다음 페이지에서 삽입된 요소를 필터링합니다.

예시:
--
```json
"filter": "20"
```
>내부 단어의 개수가 20 미만일 때 요소를 제거합니다.

```json
"filter": "😈"
```
>내부 단어에 😈가 포함되어 있을 때 요소를 제거합니다.

```json
"filter": {
    "count": 20,
    "words": "spams\\d",
    "link": "^https://spams\\.xxx",
    "selector": "div#spam"
}
```
>내부 단어가 `/spams\\d/`와 일치하거나, 내부 단어 수가 20 미만이거나, 삽입된 링크가 `/^https://spams\\.xxx/`와 일치하거나, "div#spam" 선택자와 일치하는 자식이 있는 경우 요소를 제거합니다.

```
