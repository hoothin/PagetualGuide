# pinUrl
>다음 링크 또는 페이지 요소가 존재하지 않는 경우가 있습니다. 이 값을 true로 설정하면 지능적인 규칙으로 요소를 찾는 대신 URL만으로 규칙을 고정할 수 있습니다.

예시:
--
```json
"pinUrl": true
```
>URL이 이전에 설정한 정규식(RegExp)과 일치하면, 요소가 규칙과 일치하는지 여부와 관계없이 현재 규칙이 적용됩니다.

*예를 들어, 사이트에서 스크롤하거나 무언가를 클릭했을 때만 다음 링크를 표시하는 경우, pinUrl을 true로 설정해야 합니다.*