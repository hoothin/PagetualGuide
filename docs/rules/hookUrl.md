# hookUrl
>Some non‑link elements open a URL on click and may lose that behavior after insertion. Bind a selector here to keep the click‑to‑open function.

Example:
--
```json
"hookUrl": "#click-to-open"
```
>Keep click‑to‑open behavior for elements `#click-to-open`.
