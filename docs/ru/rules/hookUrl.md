# hookUrl
>Некоторые не‑ссылки открывают URL по клику и могут потерять эту функцию после вставки. Привяжите selector, чтобы сохранить клик.

Пример:
--
```json
"hookUrl": "#click-to-open"
```
>Сохраняет клик‑функцию для элементов `#click-to-open`.
