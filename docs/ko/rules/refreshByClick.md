# refreshByClick
>제출 버튼 클릭 시 URL 변경 없이 사이트 콘텐츠가 새로고침되는 경우. 대상 버튼의 선택자로 설정하면, pagetual이 변경 사항을 감지하고 버튼 클릭 후 재설정합니다.

예시:
--
```json
"refreshByClick": "#sreach"
```
>요소 "#sreach" 클릭 시 pagetual을 다시 시작합니다.