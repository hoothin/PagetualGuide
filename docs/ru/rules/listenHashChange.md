# listenHashChange
>Установите true, чтобы Pagetual перезапускался при изменении hash.

Пример:
--
```json
"listenHashChange": true
```
>Перезапускает Pagetual, когда URL меняется с `http://abc.xxx/#123` на `http://abc.xxx/#456`.
